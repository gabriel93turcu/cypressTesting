name: Run Cypress Tests

on:
  push:
    branches:
      - main # Adjust based on your main branch
  pull_request:
    branches:
      - main # Trigger on PR to master branch

jobs:
  cypress-run:
    runs-on: ubuntu-latest # You can use macOS or Windows, depending on your environment

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "16" # Use the appropriate Node.js version for your project

      - name: Install dependencies
        working-directory: ./couse/Software-testing/cypressTesting # Correct path to package.json
        run: npm install

      - name: Run Cypress tests
        working-directory: ./couse/Software-testing/cypressTesting # Correct path to package.json
        run: npx cypress run # Or use `npx cypress open` for interactive mode
